# Tick 1 — 2026-06-15 (manual launch)

First real build tick, run by the orchestrator to launch + validate the pipeline end-to-end before handing off to the scheduled 2-hourly cadence.

## Prep
- Merged go-live config (PR #1) and green-baseline fixes (PR #2): typed Supabase cookie handlers, added `eslint-config-prettier`, dropped unused playwright stub, committed lockfile. Baseline went typecheck/lint/test green.
- Marked T-002 (DB + schema) done.

## Wave (model-routed, isolated worktrees)
| Task | Model | PR | Result |
|---|---|---|---|
| T-005 audit-log hash-chain + writer + 41 tests | **Opus** | #4 | green, merged |
| T-003 CI pipeline (lint/typecheck/test/build) | **Sonnet** | #3 | merged |
| T-032 incident + support runbooks + status stub | **Haiku** | #5 | merged |

## Verify (merged main, 67d5181 → after this record)
- `npm run typecheck` → 0 · `npm run lint` → 0 · `npm run test` → **44 passed**.

## Foundation fix
- Added `"root": true` to `.eslintrc.json` — nested agent worktrees were finding both the worktree's and the parent repo's eslintrc (the T-005 agent caught this and verified it environmental). `root:true` stops the walk-up so future worktree agents lint clean.

## Tokens
- Sub-agent output: Opus 105,098 · Sonnet 22,279 · Haiku 59,717 = **187,094** (this tick). Window well under the 800k soft cap.

## Handoff
- `fleet_status` → **RUNNING**. The scheduled tick (`hearth2home-sprint-tick`, every 2h at :07) continues from here. Next ready tasks: T-004 (auth, Opus), T-006 (config, Sonnet), T-024 (ruleset, Opus), T-033 (seed, Sonnet) — all deps met.
